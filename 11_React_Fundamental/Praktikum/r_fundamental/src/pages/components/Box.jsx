import Styles from "./Todos.module.css"

export default function Box ({user}){

return (
        <>{user.complete=true } 
        console.log="benar"
        <div className={Styles.border }>
        <p>{user.title}</p>
        </div>
        </>
)
}